# Copie para .env e preencha localmente. Não envie .env ao Git.
# Em Bash: set -a; source .env; set +a
# Este arquivo não é carregado automaticamente pelos scripts.
WALLEYE_EMAIL_FROM=''
WALLEYE_SMTP_PASSWORD=''
WALLEYE_EMAIL_TO=''
WALLEYE_SMTP_HOST='smtp.hostinger.com'
WALLEYE_SMTP_PORT='587'
